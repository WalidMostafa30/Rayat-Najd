import Loader from "./Loader";

const LoadingSection = () => {
  return (
    <article className="h-[400px] flex items-center justify-center">
      <Loader />
    </article>
  );
};

export default LoadingSection;
